This module provides a way to purge the body (content) of emails that are sent using a template.
Useful in case you want to keep track of what was sent, but don't want to store sensitive content that was in the
mail body.
